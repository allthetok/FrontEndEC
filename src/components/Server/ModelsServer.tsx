import { ProductObj } from '@/helpers/types/fetypes'
import React from 'react'

type ModelServerProps = {
	modelProducts: ProductObj[]
}

const ModelServer = ({ modelProducts }: ModelServerProps) => {
	return (
		<></>
	)
}

export { ModelServer }